# ──────────────────────────────────────────────────────────────────
# VERCEL DEPLOY için gereken env vars (Vercel > Project > Settings > Environment Variables)
# ──────────────────────────────────────────────────────────────────

# Render'da deploy edilmiş backend URL'i
# Render Dashboard > Web Service > URL alanından alın (trailing slash olmadan)
REACT_APP_API_BASE_URL=https://geosafe-backend.onrender.com
