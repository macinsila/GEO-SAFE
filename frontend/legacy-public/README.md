Legacy static HTML prototypes are archived here and are intentionally outside
`frontend/public`.

Do not deploy these files as part of the React app. They contain obsolete API
paths and pre-Sprint-1 prototype flows that are not part of the supported
security surface.
